This zip contains two files to add to your GitHub repo:

1) scripts/wmo_tracks_daily.R
2) .github/workflows/wmo_tracks_daily.yml

After unzipping into the repo root, commit and push.
Then ensure: Repo Settings → Actions → General → Workflow permissions → "Read and write permissions".
